# Cloud Build config for the `tb-analyzer` Cloud Run service.
#
# Submit with:
#   gcloud builds submit --config=cloudbuild/analyzer.yaml \
#       --substitutions=_REGION=us-central1 .

substitutions:
  _REGION: us-central1
  _SERVICE: tb-analyzer
  _REPO: tb-ai-projects

steps:
  - id: build
    name: gcr.io/cloud-builders/docker
    entrypoint: bash
    args:
      - -c
      - |
        docker build \
          -t ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest \
          projects/hate-it-rate-analyzer

  - id: push
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}

  - id: deploy
    name: gcr.io/google.com/cloudsdktool/cloud-sdk
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE}
      - --image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest
      - --region=${_REGION}
      - --platform=managed
      - --allow-unauthenticated
      - --set-secrets=ANTHROPIC_API_KEY=anthropic-api-key:latest
      - --service-account=ai-projects@threadbeast-devops.iam.gserviceaccount.com
      - --memory=1Gi
      - --cpu=1

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - ${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:latest
